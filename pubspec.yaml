name: seven_page_app
description: App คำนวณค่างวดรถ.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1 #ใช้ฟอนต์จาก google fonts
  image_picker: ^1.1.2 #ช่วยเปิดรูปจากกล้องหรือแกลอรี่
  intl: ^0.20.2 #ใช้ในการเพิ่มคอมม่าในตัวเลข

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/img01.png
    - assets/images/img02.png
    - assets/icon/icon.png